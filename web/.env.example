# Web Client Configuration

# Backend host, without a scheme. The page derives http/ws or https/wss from
# its own URL. Default: localhost:8787 (wrangler dev)
VITE_API_HOST=tiktokforwork.torubj0904.workers.dev

# Open the event log by default (default: false)
VITE_DEBUG=false
